// var Limit     = require("../src/matching_engine/limit.ts")
import Limit from "../src/matching_engine/limit";

describe('Options tests', () => { // the tests container
  it('checking default options', () => { // the single test
      const options = new Limit(1, 0); // this will be your class
  });
});